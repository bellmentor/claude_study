# 엑셀 파일 전체 (계정정보, 다운로드 매입금, 합산결과 등 — 추적 안 함)
*.xlsx
*.xls
~$*.xlsx
~$*.xls

~$*.zip

# 정산엑셀 업로드 폴더 전체 (업로드 엑셀 + _manifest.json — 로컬 런타임 데이터라 추적 안 함)
정산엑셀/

# Python 캐시
__pycache__/

# 에러 덤프 (스크린샷, HTML)
error_dumps/

# 로그인 확인용 스크린샷 (실행 시마다 갱신되는 산출물)
after_login.png

# venv
.venv/

#기타
프롬프트용.txt
기타/