== Web UI 진행상황 ==
최종 수정일: 2026-07-14

참고 이미지: ui_test/매입금수집창.png, ui_test/설정창.png

════════════════════════════════════════
[기술 스택] 확정
════════════════════════════════════════

- 백엔드: FastAPI (async)
- 프론트엔드: Jinja2 템플릿 + vanilla JS (빌드 도구 없음)
- 로그 전달: HTTP 폴링 (GET /api/collect/status?since=N, 0.8초 간격)
  - 2026-06-14 변경: WebSocket → 폴링. WebSocket은 재연결/replay가 없어
    연결이 한 번 끊기거나 늦게 붙으면 로그가 통째로 유실됐다(머신/타이밍 의존).
    서버가 로그를 버퍼(_logs)에 쌓고, 진행상황 폴링이 since 이후 로그를 함께
    내려주는 방식으로 변경 → 환경 무관하게 안정 동작.
  - /ws/logs 엔드포인트는 레거시로 남아있으나 클라이언트는 더 이상 사용 안 함.
- 의존성: fastapi, uvicorn, jinja2 (+ 기존 openpyxl, playwright, pandas)

════════════════════════════════════════
[파일 구조]
════════════════════════════════════════

app/
├── __init__.py
├── server.py          # FastAPI 앱 + 모든 API 엔드포인트 + 로그 버퍼(_logs)
│                      #  - 상단에서 stdout/stderr UTF-8 reconfigure (cp949 깨짐 방지)
│                      #  - subprocess 로그를 콘솔 print + 버퍼 append (send_log)
├── templates/
│   └── index.html     # 단일 페이지 (탭 전환은 JS)
└── static/
    ├── style.css      # UI 스타일
    └── app.js         # 탭 전환, 달력, 수집 요청, 폴링으로 로그 수신(since)

════════════════════════════════════════
[실행 방법]
════════════════════════════════════════

1. pip install fastapi uvicorn jinja2
2. python -m app.server
3. 브라우저에서 http://localhost:8000 접속

════════════════════════════════════════
[API 엔드포인트]
════════════════════════════════════════

GET  /                       : 메인 페이지
GET  /api/accounts           : 계정 목록 조회 (계정정보.xlsx)
PUT  /api/accounts           : 계정 정보 수정 → xlsx 저장
POST /api/collect            : 매입금 수집 시작 (sites[], start_date, end_date)
GET  /api/collect/status?since=N : 진행상황 + N 인덱스 이후 새 로그(logs, log_count) 반환
GET  /api/settlement                 : 정산엑셀 슬롯 목록+업로드 상태 반환
POST /api/settlement/upload/{key}    : 정산엑셀 업로드 (raw body 바이트, 원본파일명은 X-Filename 헤더)
DEL  /api/settlement/{key}           : 정산엑셀 삭제
  ※ 파일명을 쿼리 대신 X-Filename 헤더로 보냄 → uvicorn 접속로그에 %인코딩 한글 안 찍힘.
    업로드/삭제 시 서버 콘솔에 '[정산엑셀] ... 업로드됨' 읽기 쉬운 한글 로그도 print.
GET  /api/aejulnun                   : 에이준줄눈 작업폴더 상태(선택폴더명/엑셀목록) 반환
POST /api/aejulnun/clear             : 새 폴더 시작 — 작업폴더 비우고 메타 초기화 (폴더명은 X-Folder 헤더)
POST /api/aejulnun/upload            : 폴더 안 엑셀 1개 업로드 (raw body, 상대경로는 X-Filename 헤더)
GET  /api/aejulnun/master-sheets     : 정산엑셀관리 total.xlsx 의 시트명 목록 반환(첫/마지막 보정용)
POST /api/aejulnun/calc              : 발주서 파일별 계산 (body {year,month,sheet}) →
                                       {results:[{name,amount,error}], total, month, master_note}
DEL  /api/aejulnun                   : 에이준줄눈 작업폴더 비우기
  ※ 폴더 업로드 흐름: JS 가 폴더 선택(webkitdirectory/드래그) → clear → 각 엑셀 upload 순차 → GET 갱신.
    엑셀(.xlsx/.xls) 아닌 파일은 서버가 무시. 저장: 정산엑셀/aejulnun/, 메타: _meta.json.
    server.aejulnun_files() 로 다음 계산단계가 폴더 안 엑셀 경로들을 읽어 씀.
WS   /ws/logs                : (레거시) 클라이언트 미사용

════════════════════════════════════════
[화면 구성] 4개 탭  (네비 순서: 정산엑셀관리 | 매입금수집 | 에이준줄눈 | 설정)
════════════════════════════════════════

0. 정산엑셀관리 탭 (좌측 네비, 2026-07-05 추가)
   - 프로그램이 계속 참조할 정산용 엑셀을 명명된 슬롯에 업로드/교체/삭제
   - 슬롯별 카드(칸칸 구분: 헤더 띠 + 좌측 강조바): 드래그앤드롭 + [파일 찾기] + 현재파일 + [삭제]
     · 드래그 시 드롭존 색이 확 바뀜(바닐라, 라이브러리 없음)
   - 저장: 정산엑셀/<key>.xlsx (고정경로), 표시 메타는 정산엑셀/_manifest.json
   - 슬롯 정의는 server.py 의 SETTLEMENT_SLOTS. 현재: total=전체 정산용 엑셀 (1개)
     · '에이준 줄눈' 슬롯은 제거됨 → 에이준줄눈은 아래 '에이준줄눈' 탭(폴더 방식)에서 처리
   - 업로드는 python-multipart 없이 raw body 방식(request.body()). 확장자 .xlsx/.xls 만 허용
   - server.settlement_file_path(key) 로 다른 코드가 업로드 엑셀 경로를 얻어 read_excel 가능
   - 정산엑셀/ 폴더는 .gitignore 로 통째로 제외(엑셀+manifest 로컬 전용)

0-2. 에이준줄눈 탭 (매입금수집 오른쪽, 2026-07-05 추가)
   - 여러 엑셀이 든 '폴더'를 드래그앤드롭 또는 [폴더 찾기](webkitdirectory)로 선택
   - 브라우저가 폴더 절대경로를 못 주므로, 선택 폴더의 엑셀들을 서버 작업폴더(정산엑셀/aejulnun/)로 복사
     · JS: clear → 각 엑셀 upload 순차 → 목록 갱신. 엑셀 아닌 파일은 무시. 하위폴더는 __ 로 평탄화.
   - 화면: 선택폴더명/엑셀개수/시각 + [비우기]. 파일목록은 스크롤 박스(헤더 sticky).
   - 계산 설정 바: [계산 월(년/월)] + [정산엑셀관리 불러오기]→시트 드롭박스 + [계산하기](오른쪽).
     · 계산 월 기본값은 폴더명('26년 6월')에서 파싱.
   - ★ 계산 규칙 구현됨 (2026-07-05, 프롬프트용.txt '# 에이준줄눈 매입금 계산 방법'):
     · 발주서 파싱(_parse_order_form): '상품명'~'택배사명' 사이 가격블록 합=순액,
       상품명열의 'A+B=C' 요약행에서 순액 A/합계 C 파싱. (검증: 0601 순액 219,200 일치)
       ★ 요약행에서 파싱 종료(break) — 그 아래 '2/28 미수금 39,050 포함' 같은 메모 행을 주문으로
         잘못 합산하던 버그 수정(2026-07-05, 3월 첫파일 0303 에서 39,050 뻥튀기 발견).
     · 중간 날짜 파일 = 요약행 합계 C(부가세 포함) 그대로.
     · 첫/마지막 날짜 파일(파일명 MMDD 최소/최대) = 정산엑셀관리 total.xlsx 선택 시트에서
       수령자명 매칭으로 보정. ★ 매칭 날짜윈도우는 '달력 1일/말일'이 아니라 '실제 파일 날짜' 기준:
         · 첫 파일: 대상월 1일 ~ '첫 파일 날짜'(예 폴더가 0102부터면 1/1~1/2) 주문자만 남김(전달분 제거).
         · 마지막 파일: '마지막에서 두번째 파일 날짜' ~ 대상월 말일(예 …0130,0202면 1/30~1/31) 주문자만 남김(다음달분 제거).
       (_master_recip_map={일:수령자set}, _recips_in_days 로 윈도우 합집합). 남은 순액 재계산 → +부가세(반올림).
       또 가격블록합 vs 요약행순액 대조해 다르면 결과표 '에러'열에 기입.
     · ★ total.xlsx(정산엑셀) 구조: 시트 원본/대량/줄눈/중간계산/최종정산. 주문시트는 헤더가 0행이 아니라
       수령자 키워드로 헤더행 자동탐지. 수령자 컬럼='수령자'(발주서 '수령인명'과 동일인), 날짜='주문일자(yyyy-MM-dd)'.
       에이준줄눈은 '줄눈' 시트 선택. (_MASTER_RECV_KW/_MASTER_DATE_KW 상수)
     · 결과표: 파일이름 | 매입금 | 에러 + 합계행. total.xlsx 없거나 시트 미선택이면 첫/마지막은
       요약행 합계로 대체하고 master_note 배너로 안내.
     · ★ 상세보기(2026-07-05): 첫/마지막 파일 행(▸)을 클릭하면 아래로 펼쳐져 검증 내역 표시 —
       적용 규칙(예 '3/1~3/3'), 파일 전체순액, [남긴 주문 표]/[제거됨(전달분/다음달분) 표],
       '순액+부가세=매입금' 줄. calc 응답의 result.detail{edge,window,full_net,kept,removed,net,vat}로 렌더.
       중간 파일은 detail=null(클릭 불가).
   - 검증(2026-01, 줄눈 시트, 폴더 0102~0202): 첫 0102=257,840, 마지막 0202=124,850 (수기 일치).
   - 검증(2026-03, 줄눈 시트, 폴더 0303~0401): 첫 0303=213,400, 마지막 0401=63,250 (수기 일치).
     · 첫 0303 은 맨아래 메모행('2/28 미수금 39,050') 때문에 틀렸었음 → 요약행 break 로 해결.
     · 매칭은 수령자명(송장번호도 시도했으나 마지막 파일에서 값이 달라져 수령자명 유지).
   - 검증(2026-06, 줄눈 시트): 첫 0601=43,670, 마지막 0701=25,300, 월 총 1,658,140원.
   - ★ 첫/마지막 보정을 '달력 1일/말일'로 했다가 1월(첫파일 0102, 끝파일 0202)에서 틀림 →
     '실제 파일 날짜 윈도우'로 수정(2026-07-05). 6월은 첫=0601·끝직전=0630 이라 우연히 값 동일.

1. 매입금 수집 탭 (메인, 중앙 네비)
   - 좌측: 달력 (월/년 드롭다운, 날짜 범위 선택)
   - 중앙: 도매사이트 테이블 (체크박스/사이트명/매입금/진행상황/메모/에러)
   - 우측: [수집시작] 버튼
   - 하단: 실시간 로그

2. 설정 탭 (우측 네비)
   - 계정 설정 테이블 (사이트/아이디/비번) + 추가/삭제/저장
   - 하단: 로그

════════════════════════════════════════
[구현 진행상황]
════════════════════════════════════════

[O] 기술 스택 확정 (FastAPI + Jinja2 + vanilla JS)
[O] 프로젝트 구조 생성 (app/ 폴더)
[O] 프론트엔드 HTML/CSS/JS
    [O] 상단 네비 (탭 전환)
    [O] 매입금 수집 탭 - 달력 위젯 (범위 선택)
    [O] 매입금 수집 탭 - 도매사이트 테이블
    [O] 매입금 수집 탭 - 수집시작 버튼 + 로직
    [O] 설정 탭 - 계정 테이블 (CRUD)
    [O] 하단 로그 표시 영역
[O] 백엔드 API
    [O] 수집 시작 엔드포인트 (POST /api/collect)
    [O] 진행상황 조회 엔드포인트 (GET /api/collect/status)
    [O] 계정 CRUD 엔드포인트 (GET/PUT /api/accounts)
    [O] 로그 전달 (HTTP 폴링 + 서버 로그 버퍼, 2026-06-14 WebSocket에서 전환)
[O] 기존 dome_site 모듈 연동 (subprocess 방식: python -m dome_site.<slug>.main)
    [O] 오너클랜 연동 — 테스트 완료 (5월 매입금 892,830원 확인)
    [O] 젠트레이드 연동 — 모듈 구현 완료, 테스트 완료 (5월 매입금 230,240원 확인, 2026-06-13)
    [ ] 기타 도매처 연동 (모듈 구현 후 자동 연동)
[O] API 테스트 완료 (수집 흐름 end-to-end, 2026-06-13)
[O] 브라우저에서 실제 UI 동작 확인 (수집+로그 한글 정상 표시, 2026-06-13)
[O] 로그 유실 버그 수정 (2026-06-14)
    - 증상: 데스크톱에서 수집/매입금은 뜨는데 로그만 안 뜸 (노트북은 정상)
    - 원인: WebSocket 단일 채널 의존(재연결·replay 없음) → 연결 삐끗하면 로그 전손.
            HTTP 폴링은 두 머신 다 정상이었음(매입금 표시가 증거).
    - 조치: 서버 로그 버퍼 + 폴링 전달로 변경. WebSocket 클라이언트 코드 제거.
    - 주의: app.js는 정적파일이라 브라우저 캐시 영향 → 적용 후 Ctrl+F5 강력새로고침 필요.
[ ] UI 세부 조정 (디자인 그림과 비교)

[O] 수집 테이블 컬럼 확장: 메모 / 에러 추가 (2026-07-04)
    - 테이블 컬럼: [체크] 도매사이트 / 매입금 / 진행상황 / 메모 / 에러
    - 메모(col-memo): 사이트별 '항상 표시' 고정 안내. server.py 의 SITE_NOTES(slug→문구)에
      정의. 현재: coms="페이지 넘어갈시 코드수정필요". load_site_list()가 note 필드 부여 →
      index.html 이 col-memo 에 <span class="site-note">(빨강)로 렌더. 새 사이트 안내는
      SITE_NOTES 에 slug 한 줄 추가.
    - 진행상황(col-status): 동적 상태('실행 중...'/'완료'/'오류'). <span class="status-text">.
    - 에러(col-error): 실행 오류 발생 시 채워짐. _collect_status[slug]['error'] 로 전달,
      <span class="error-text">(빨강). 정상일 땐 빈칸.
      · 서브프로세스 종료코드≠0 → status='오류', error='오류 (종료코드: N)'.
    - app.js 는 상태 갱신 시 .status-text/.error-text 만 건드려 메모(.site-note)는 안 지움.
    - 주의: app.js/style.css 정적파일 캐시 → 적용 후 Ctrl+F5 강력새로고침 필요.
